# Keep files local - do not push to GitHub
*.txt
!README.md

# Commit message file (local only)
COMMIT.md

# Vector database (generated locally, too large for git)
chroma_langchain_db/

# Environment variables with API keys
.env

# Python virtual environment (generated locally)
.venv/

# Research data folder (never stage)
research/

# Copilot instructions (workspace-specific, keep local)
.github/copilot-instructions.md
